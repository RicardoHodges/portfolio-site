Portfolio site (Next.js + Tailwind)\n\nQuick start:\n1. npm install\n2. cp .env.example .env.local and set SMTP / RECAPTCHA as needed\n3. npm run dev\n\nNotes:\n- Dark mode is default (class-based).\n- Contact API supports SMTP via env vars; otherwise logs locally.\n- Ensure RECAPTCHA_SECRET if you enable reCAPTCHA (server-side validation).\n- Tailor project entries and personal copy to avoid generic language and emphasize impact.
